---
name: Push

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v2
      
      - name: Print env
      - run: |
          echo "Print all environment variables"
          env
      
      - name: Validate branch name
        run: ./.github/scripts/validate-branch-name.sh

      - name: Say Maintenance-issue if
        if: startsWith(github.ref.name, 'maintenance')
        run: |
          echo "only maintenance, do not deploy"
      
      - name: Say Deploy
        if: startsWith(github.ref.name, 'maintenance') != true
        run: |
          echo "not maintenance, deploy"
        